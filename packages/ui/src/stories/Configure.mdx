import { Meta } from "@storybook/blocks";


<Meta title="Moviired UI" />

<main class="animate-slide-up">
  <h1>Moviired UI</h1>

  <p>
    Una experiencia visual sólida y accesible que conecta tecnología y simplicidad para facilitar servicios financieros en cada interacción.
  </p>

  <img 
      src="/laptop.png"
      alt="Mobile Interface" 
    />

</main>


<style>
{`
  @keyframes fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes slide-up {
    from { transform: translateY(20px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
  }

  @keyframes slide-left {
    from { transform: translateX(20px); opacity: 0; }
    to { transform: translateX(0); opacity: 1; }
  }

  @keyframes slide-right {
    from { transform: translateX(-20px); opacity: 0; }
    to { transform: translateX(0); opacity: 1; }
  }

  .animate-fade-in {
    animation: fade-in 0.6s ease-out;
  }

  .animate-slide-up {
    animation: slide-up 0.6s ease-out;
  }

  .animate-slide-left {
    animation: slide-left 0.6s ease-out;
  }

  .animate-slide-right {
    animation: slide-right 0.6s ease-out;
  }

  .w-full {
    width: 100%;
  }

  .h-full {
    height: 100%
  }

  h1 {
    color: #de2887 !important;
  }

  p {
    font-size:1.2rem !important;
  }

  img {
    width: clamp(15rem, 5vw, 950px)
    height: clamp(10rem, 4vw, 650px)
  }

`}
</style>