import { BankingProductLayout } from "@/layouts/dashboard/banking/BankingProductLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <BankingProductLayout children={children} />;
}
